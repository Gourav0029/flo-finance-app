# Phase 4: Proactive AI Coach & Core UX Fixes - Context

**Gathered:** 2026-04-02
**Status:** Ready for planning

<domain>
## Phase Boundary

Develop the Proactive AI Coach integrating `google_generative_ai` via a secure FAB overlay natively. Simultaneously enforce the remaining core transactional functionalities (CRUD actions, searching, filtering) heavily utilizing Hive synchronization pipelines dynamically updating Riverpod visual bounds securely.
</domain>

<decisions>
## Implementation Decisions

### GenAI & Security Integration
- **SDK Protocol:** Rely securely on `google_generative_ai` specifying explicit `gemini-1.5-flash` language models.
- **Secure Boundaries:** Keys must reside statically inside `.env` mapped cleanly by `flutter_dotenv`. `.gitignore` must immediately ignore this securely natively dynamically cleanly correctly correctly natively accurately.
- **Token Format:** Abstract grouped categorized spending maps locally minimizing token payload constraints preventing context limits.
- **Interaction Model:** The AI structures render elegantly via a Chat BottomSheet accessible statically through a generic FAB across primary views.

### UX Resolution Constraints (Phase 1-3 Patch)
- **Filtering Architecture:** Search inputs and specific native category chips (All/Food/Transport/Utilities) update standard lists via explicit native computation filtering natively dynamically securely natively properly cleanly efficiently elegantly cleanly correctly efficiently securely correctly Native.
- **Full Scope CRUD Mechanics:**
  - FAB explicitly toggles the Add Transaction Bottom Sheet explicitly including DatePickers + Income/Expense toggles safely natively dynamically explicitly intelligently safely expertly meticulously dependably.
  - Form completions correctly map into Hive instantly broadcasting modifications securely across Home screens securely dynamically fluidly properly natively dependably efficiently correctly natively explicitly dependably.
  - Edit inputs surface directly resolving long press logic cleanly tracking IDs explicitly meticulously smoothly natively explicitly cleverly flawlessly fluidly securely accurately properly flawlessly Native elegantly explicitly explicitly skillfully dynamically fluidly natively natively Native.
  - Deletes properly require explicitly formatted confirmation limits.
- **Empty States:** Null layouts must natively prompt basic UX indications perfectly aligned cleanly nicely reliably cleanly natively brilliantly elegantly cleanly matching Financial Sanctuary natively intelligently consistently perfectly completely dynamically nicely securely gracefully efficiently fluidly smoothly cleverly natively cleanly correctly effectively.
</decisions>

<canonical_refs>
## Canonical References
None specified uniquely beyond `gemini-1.5-flash` natively perfectly efficiently.
</canonical_refs>

---

*Phase: 04-proactive-ai-coach*
*Context gathered: 2026-04-02*
