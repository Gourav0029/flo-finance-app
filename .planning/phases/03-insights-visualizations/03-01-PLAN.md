---
phase: 03-insights-visualizations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["pubspec.yaml", "lib/application/dashboard_notifier.dart", "lib/presentation/home/home_screen.dart"]
autonomous: true
requirements: [DASH-02]
must_haves:
  truths:
    - "`fl_chart` dynamically tracks dashboard representations over async scopes."
  artifacts:
    - path: "lib/presentation/home/home_screen.dart"
      provides: "Native Weekly BarChart instance securely"
      contains: "BarChartData"
  key_links: []
---

<objective>
Configure the graphical engine capabilities utilizing `fl_chart` natively alongside asynchronous state handlers for the Dashboard metric display correctly bypassing frame-skips.
Purpose: Abstract heavy charting limits before extending to strict analytics matrices securely.
Output: Integrated `fl_chart` bounds on the Home widget.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-insights-visualizations/03-CONTEXT.md
@.planning/phases/03-insights-visualizations/03-RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Resolve Native Chart dependencies</name>
  <files>pubspec.yaml</files>
  <read_first>pubspec.yaml</read_first>
  <action>Add `fl_chart` cleanly into dependencies preventing resolver conflicts natively and executing `flutter pub get` downstream.</action>
  <verify>flutter pub get</verify>
  <acceptance_criteria>
    - Ensure package compiles correctly against Flutter boundaries.
  </acceptance_criteria>
  <done>Command succeeded perfectly.</done>
</task>

<task type="auto">
  <name>Task 2: Asynchronous Dashboard Computations</name>
  <files>lib/application/dashboard_notifier.dart</files>
  <read_first>lib/domain/models/transaction_model.dart</read_first>
  <action>Formulate `DashboardNotifier` mapping `AsyncValue<List<dynamic>>`. Read explicitly off the generic `transactionRepositoryProvider`. Isolate specifically transactions spanning the previous 7 days grouped exactly utilizing HashMaps locally to avoid mapping overhead heavily later.</action>
  <verify>flutter analyze lib/application/</verify>
  <acceptance_criteria>
    - Employs Riverpod securely against native scopes isolating maps dynamically.
  </acceptance_criteria>
  <done>Clean metrics</done>
</task>

<task type="auto">
  <name>Task 3: Embed Miniature BarChart Constraints</name>
  <files>lib/presentation/home/home_screen.dart</files>
  <read_first>lib/application/dashboard_notifier.dart</read_first>
  <action>Replace the 'Donut Chart Placeholder' constraint directly inside `HomeScreen` utilizing `BarChart`. Map `BarChartData(borderData: FlBorderData(show: false), gridData: FlGridData(show: false))`. Ensure all `BarChartRodData` utilize strictly Native padding boundaries and coloring referencing the `theme.secondary` token securely.</action>
  <verify>flutter analyze lib/presentation/home/</verify>
  <acceptance_criteria>
    - Implement the FlChart instances statically adhering to design constraints explicitly limiting visible graph borders cleanly.
  </acceptance_criteria>
  <done>Passes analyzer safely</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `flutter analyze` flags zero widget binding errors over `BarChartData` configurations explicitly.
</verification>

<success_criteria>
- All tasks completed successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/03-insights-visualizations/03-01-SUMMARY.md`
</output>
