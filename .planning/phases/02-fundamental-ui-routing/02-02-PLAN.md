---
phase: 02-fundamental-ui-routing
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: ["lib/presentation/home/home_screen.dart", "lib/presentation/widgets/glass_nav.dart"]
autonomous: true
requirements: [DASH-01, DASH-03]
must_haves:
  truths:
    - "Home Dashboard renders native widget composites safely."
    - "Glassmorphism NavBar maps dynamically over ShellRoute routing index."
  artifacts:
    - path: "lib/presentation/home/home_screen.dart"
      provides: "Dashboard balance composites"
      contains: "Balance"
    - path: "lib/presentation/widgets/glass_nav.dart"
      provides: "Persistent BackdropFilter rendering system"
      contains: "BackdropFilter"
  key_links: []
---

<objective>
Execute the core display artifacts for the primary Dashboard rendering sequence and abstract the application's persistent navigational UI shell.
Purpose: Converts raw Stitch abstractions into the central data hub rendering target.
Output: Complete glassmorphism bottom tabs acting over the StatefulShellRoute along with Home composite panels.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-fundamental-ui-routing/02-CONTEXT.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Assemble Glass Nav component</name>
  <files>lib/presentation/widgets/glass_nav.dart</files>
  <read_first>lib/core/theme/app_theme.dart</read_first>
  <action>Create a custom `GlassNav` widget accepting `currentIndex` and an `onTap(index)` callback. Utilize `ClipRRect` mapping a `BackdropFilter` (blur: 20x20) wrapping a pure Container (color: White at 80% opacity) containing the 4 primary app Tab icons configured via Flex mappings across Cupertino icons.</action>
  <verify>flutter analyze lib/presentation/widgets/</verify>
  <acceptance_criteria>
    - Contains `BackdropFilter(filter: ImageFilter.blur`
  </acceptance_criteria>
  <done>Code matches syntax specification</done>
</task>

<task type="auto">
  <name>Task 2: Architect Home Dashboard display grid</name>
  <files>lib/presentation/home/home_screen.dart</files>
  <read_first>lib/presentation/widgets/glass_nav.dart</read_first>
  <action>Create `HomeScreen` (ConsumerWidget). Render a standard overarching `Scaffold`. Mount a centralized `total gross balance` card prominently employing the `AppTheme.primary` navy color with `BorderRadius.circular(24)`. Scaffold income/expense horizontal partitions below it. Use standard dummy static constraints temporarily (to be hooked to repository in future wave).</action>
  <verify>flutter analyze lib/presentation/home/</verify>
  <acceptance_criteria>
    - Card integrates `BorderRadius.circular(24)` explicitly.
  </acceptance_criteria>
  <done>Widget tree is structurally sound</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] Ensure 24px structural border constraints abide by Phase 2 decisions.
</verification>

<success_criteria>
- All tasks completed
</success_criteria>

<output>
After completion, create `.planning/phases/02-fundamental-ui-routing/02-02-SUMMARY.md`
</output>
