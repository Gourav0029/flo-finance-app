---
phase: 02-fundamental-ui-routing
plan: 04
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified: ["lib/application/transaction_form_notifier.dart", "lib/presentation/transactions/widgets/add_transaction_bottom_sheet.dart"]
autonomous: true
requirements: [TXN-03, TXN-04]
must_haves:
  truths:
    - "Add transaction component accurately collects generic input limits."
    - "Riverpod Notifier successfully guards the data serialization states."
  artifacts:
    - path: "lib/application/transaction_form_notifier.dart"
      provides: "Validation and staging proxy"
      contains: "@riverpod"
    - path: "lib/presentation/transactions/widgets/add_transaction_bottom_sheet.dart"
      provides: "Bottom sheet input UX"
      contains: "TextFormField"
  key_links: []
---

<objective>
Provide Riverpod-centric form handlers coupled seamlessly into a modern sliding Bottom Sheet view capturing input additions interactively.
Purpose: Converts user intents cleanly over to Hive bounds through separated validational states.
Output: Reusable form builder BottomSheet bound exclusively to pure state Notifiers.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-fundamental-ui-routing/02-CONTEXT.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Generate riverpod Form Validation engine</name>
  <files>lib/application/transaction_form_notifier.dart</files>
  <read_first>lib/domain/models/transaction_model.dart</read_first>
  <action>Create a strongly typed Riverpod `AutoDisposeNotifier` named `TransactionFormNotifier`. Establish fields tracking inputs accurately utilizing immutable standard State configurations dynamically capturing `amount`, `category`, and `date` with standard basic text validation functions before communicating natively to the generic repository downstream.</action>
  <verify>flutter analyze lib/application/</verify>
  <acceptance_criteria>
    - Exports a Riverpod Notifier safely handling data types natively without UI imports.
  </acceptance_criteria>
  <done>No standard coupling violations exist between dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Construct the animated Bottom Sheet Form</name>
  <files>lib/presentation/transactions/widgets/add_transaction_bottom_sheet.dart</files>
  <read_first>lib/application/transaction_form_notifier.dart</read_first>
  <action>Develop a pure flutter `ConsumerWidget` rendering the Add Transaction fields via `TextFormField` parameters mapped iteratively targeting the above Notifier. Encapsulate execution tightly so it seamlessly slides via `showModalBottomSheet()` directly operating atop the main ShellRoute.</action>
  <verify>flutter analyze lib/presentation/transactions/widgets/</verify>
  <acceptance_criteria>
    - Contains multiple `TextFormField` implementations tracking correctly.
  </acceptance_criteria>
  <done>Code is pristine targeting Flutter 3 forms natively.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] No `setState()` bounds exist unnecessarily bridging logic limits
</verification>

<success_criteria>
- All tasks completed
</success_criteria>

<output>
After completion, create `.planning/phases/02-fundamental-ui-routing/02-04-SUMMARY.md`
</output>
