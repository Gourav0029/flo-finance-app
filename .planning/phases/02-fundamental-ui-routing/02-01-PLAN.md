---
phase: 02-fundamental-ui-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["pubspec.yaml", "lib/core/theme/app_theme.dart", "lib/routing/app_router.dart", "lib/main.dart"]
autonomous: true
requirements: [ARCH-01]
must_haves:
  truths:
    - "GoRouter ShellRoute manages multi-tab navigation natively."
    - "The 'Financial Sanctuary' colors are registered in ThemeData."
  artifacts:
    - path: "lib/routing/app_router.dart"
      provides: "GoRouter StatefulShellRoute implementation"
      contains: "StatefulShellRoute"
    - path: "lib/core/theme/app_theme.dart"
      provides: "ThemeExtension encapsulating strict Stitch colors"
      contains: "0xFF1E1B4B"
  key_links: []
---

<objective>
Configure the complete application routing skeleton and global design theme bindings.
Purpose: Locks the core foundation before complex data-driven screens are mounted on it.
Output: Integrated `GoRouter`, Riverpod routing hooks, and static ThemeData objects.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-fundamental-ui-routing/02-CONTEXT.md
@.planning/phases/02-fundamental-ui-routing/02-RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add standard frontend plugins</name>
  <files>pubspec.yaml</files>
  <read_first>pubspec.yaml</read_first>
  <action>Add `go_router` and `google_fonts` to dependencies. Run `flutter pub get`.</action>
  <verify>flutter pub get</verify>
  <acceptance_criteria>
    - pubspec.yaml contains "go_router:"
  </acceptance_criteria>
  <done>Command succeeded</done>
</task>

<task type="auto">
  <name>Task 2: Financial Sanctuary Design Tokens</name>
  <files>lib/core/theme/app_theme.dart</files>
  <read_first>pubspec.yaml</read_first>
  <action>Create a class exposing the "Financial Sanctuary" design constants safely. Variables: `primary: Color(0xFF1E1B4B)`, `secondary: Color(0xFF006C4B)`, `accent: Color(0xFF64F9BC)`, `background: Color(0xFFF8F9FA)`. Ensure `ThemeData` generation uses `GoogleFonts.manropeTextTheme()` and `GoogleFonts.interTextTheme()`.</action>
  <verify>flutter analyze lib/core/theme/</verify>
  <acceptance_criteria>
    - File contains `Color(0xFF1E1B4B)`
  </acceptance_criteria>
  <done>Code passes analysis</done>
</task>

<task type="auto">
  <name>Task 3: StatefulShellRoute Abstraction</name>
  <files>lib/routing/app_router.dart</files>
  <read_first>lib/core/theme/app_theme.dart</read_first>
  <action>Create a `goRouterProvider` (using Riverpod). Inside it, return a `GoRouter` containing a `StatefulShellRoute.indexedStack`. Configure 4 branches matching our navigation intents (Home, Transactions, Goals, Insights) pointing to temporary Scaffold text placeholders.</action>
  <verify>flutter analyze lib/routing/</verify>
  <acceptance_criteria>
    - Code contains `StatefulShellRoute.indexedStack`
  </acceptance_criteria>
  <done>Code parses successfully</done>
</task>

<task type="auto">
  <name>Task 4: Integrate Router in main.dart</name>
  <files>lib/main.dart</files>
  <read_first>lib/routing/app_router.dart, lib/main.dart</read_first>
  <action>Modify `FloFinanceApp` to be a `ConsumerWidget`. Obtain the `GoRouter` instance via `ref.watch(goRouterProvider)`. Replace `MaterialApp` with `MaterialApp.router` configuring the `routerConfig` correctly along with the `AppTheme` injected.</action>
  <verify>flutter analyze lib/main.dart</verify>
  <acceptance_criteria>
    - main.dart utilizes `MaterialApp.router` instead of standard MaterialApp.
  </acceptance_criteria>
  <done>App renders clean analysis.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `flutter analyze` shows zero errors across `lib/routing/`
</verification>

<success_criteria>
- All tasks completed
- GoRouter boots without exceptions internally.
</success_criteria>

<output>
After completion, create `.planning/phases/02-fundamental-ui-routing/02-01-SUMMARY.md`
</output>
