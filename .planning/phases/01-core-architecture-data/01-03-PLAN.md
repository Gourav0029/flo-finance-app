---
phase: 01-core-architecture-data
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: ["lib/domain/repositories/transaction_repository.dart", "lib/infrastructure/hive_transaction_repository.dart", "lib/main.dart"]
autonomous: true
requirements: [DATA-02, DATA-03]
must_haves:
  truths:
    - "App boots synchronously initializing Hive."
    - "Repository exposes standard CRUD futures."
  artifacts:
    - path: "lib/infrastructure/hive_transaction_repository.dart"
      provides: "Concrete DB integration"
      contains: "implements TransactionRepository"
    - path: "lib/main.dart"
      provides: "Boot sequence router"
      contains: "await Hive.initFlutter()"
  key_links:
    - from: "lib/main.dart"
      to: "lib/infrastructure/hive_transaction_repository.dart"
      via: "Riverpod ProviderScope overrides"
      pattern: "transactionRepositoryProvider.overrideWithValue"
---

<objective>
Implement the base Transaction storage and repository abstractions, and integrate them into the app boot sequence via Riverpod.
Purpose: Enforce DI and synchronous availability for offline transactions.
Output: A working Riverpod ProviderScope and functional main() setup.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-core-architecture-data/01-CONTEXT.md
@.planning/phases/01-core-architecture-data/01-02-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Define TransactionRepository interface</name>
  <files>lib/domain/repositories/transaction_repository.dart</files>
  <read_first>lib/domain/models/transaction_model.dart</read_first>
  <action>Create `abstract interface class TransactionRepository` defining the CRUD methods: `addTransaction`, `updateTransaction`, `deleteTransaction` (taking String id), and `getAllTransactions` (returning List). Also use riverpod_annotation to generate a provider throwing UnimplementedError for dependency injection overrides.</action>
  <verify>cat lib/domain/repositories/transaction_repository.dart</verify>
  <acceptance_criteria>
    - file contains `abstract interface class TransactionRepository`
    - creates a provider `@riverpod TransactionRepository transactionRepository(TransactionRepositoryRef ref)`
  </acceptance_criteria>
  <done>Code created properly</done>
</task>

<task type="auto">
  <name>Task 2: Implement HiveTransactionRepository</name>
  <files>lib/infrastructure/hive_transaction_repository.dart</files>
  <read_first>lib/domain/repositories/transaction_repository.dart</read_first>
  <action>Create `class HiveTransactionRepository implements TransactionRepository`. It should accept a `Box<Transaction>` in its constructor and implement the CRUD abstractions synchronously with Hive's capabilities.</action>
  <verify>cat lib/infrastructure/hive_transaction_repository.dart</verify>
  <acceptance_criteria>
    - file contains `implements TransactionRepository`
  </acceptance_criteria>
  <done>Code matches interface</done>
</task>

<task type="auto">
  <name>Task 3: Refactor App Initialization (main.dart)</name>
  <files>lib/main.dart</files>
  <read_first>lib/main.dart</read_first>
  <action>Refactor `main()` to async. Call `WidgetsFlutterBinding.ensureInitialized();`. Await `Hive.initFlutter()`. Register `TransactionAdapter()`. Open the `transactionsBox` synchronously. Wrap the app's `runApp()` with a `ProviderScope`, overriding the `transactionRepositoryProvider` with the instantiated `HiveTransactionRepository` wrapping the open box.</action>
  <verify>flutter analyze</verify>
  <acceptance_criteria>
    - `main.dart` contains `await Hive.initFlutter()`
    - `main.dart` contains `ProviderScope`
    - `main.dart` contains `overrideWithValue`
  </acceptance_criteria>
  <done>flutter analyze passes</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `dart run build_runner build -d` runs to process Riverpod annotations
- [ ] `flutter analyze` shows no critical errors in main flow
</verification>

<success_criteria>
- All tasks completed
- App executes compilation without throwing
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-architecture-data/01-03-SUMMARY.md`
</output>
