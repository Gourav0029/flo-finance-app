---
phase: 01-core-architecture-data
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: ["lib/domain/models/transaction_model.dart"]
autonomous: true
requirements: [DATA-01]
must_haves:
  truths:
    - "Transaction models generate proper Hive TypeAdapters."
  artifacts:
    - path: "lib/domain/models/transaction_model.dart"
      provides: "Transaction domain model."
      contains: "@HiveType"
  key_links: []
---

<objective>
Define Hive TypeAdapters and Transaction Dart models.
Purpose: Secure the static typings of the local database so CRUD actions are safe.
Output: Strongly typed domain models with `.g.dart` generated files.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-core-architecture-data/01-CONTEXT.md
@.planning/phases/01-core-architecture-data/01-RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create Transaction Model</name>
  <files>lib/domain/models/transaction_model.dart</files>
  <read_first>pubspec.yaml</read_first>
  <action>Create class `Transaction` with fields: `id` (String), `amount` (double), `category` (String), `date` (DateTime), `notes` (String). Annotate the class with `@HiveType(typeId: 0)` and annotate each field with `@HiveField(index)`. Include `part 'transaction_model.g.dart';`.</action>
  <verify>cat lib/domain/models/transaction_model.dart</verify>
  <acceptance_criteria>
    - file contains "@HiveType(typeId: 0)"
    - file contains "part 'transaction_model.g.dart';"
  </acceptance_criteria>
  <done>Code compiles</done>
</task>

<task type="auto">
  <name>Task 2: Generate TypeAdapters</name>
  <files>lib/domain/models/transaction_model.dart</files>
  <read_first>lib/domain/models/transaction_model.dart</read_first>
  <action>Run `dart run build_runner build -d` to generate the `.g.dart` file for Hive.</action>
  <verify>ls lib/domain/models/</verify>
  <acceptance_criteria>
    - `lib/domain/models/transaction_model.g.dart` exists on disk.
  </acceptance_criteria>
  <done>build_runner succeeds with exit 0</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `dart run build_runner build -d` passes without errors
</verification>

<success_criteria>
- All tasks completed
- Generates TypeAdapter automatically
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-architecture-data/01-02-SUMMARY.md`
</output>
